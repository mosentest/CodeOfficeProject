jira:
	general configuration(enable/disable unassigned lead/reporter)
	user groups(permissions scheme)
	project-type -> configure project, different workflow, different issue type schemes(bug, new feature, etc.)
	editing issue type (default issue types stores in database, not as enum class)
	workflow management,
	add/edit component in components
	more project management authorities